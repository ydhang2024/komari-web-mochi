import { Flex } from "@radix-ui/themes";

const CommandClipboard = ({ ...props }: { [key: string]: any }) => {
  return (
    <Flex {...props} direction="column" gap="2" className="command-clipboard-container">
      Work in progress...
    </Flex>
  );
};


export default CommandClipboard;
